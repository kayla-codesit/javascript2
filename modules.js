/**Modules are a javascript file that can have a function */
/*More for single page application. Have dynamic content in a module that updates iwthin the application. Used in react library. modules are small pieces of code. */
export function sayHi(user) {
    alert(`Hello ${user}`);
}

export function encourage(user) {
    alert(`You're doing great ${user}! Keep up the good work!`);
}